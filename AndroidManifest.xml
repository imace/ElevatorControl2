<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.kio.ElevatorControl"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk
            android:minSdkVersion="14"
            android:targetSdkVersion="19"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:hardwareAccelerated="true"
            android:theme="@style/Theme.Elevator.Control">
        <!-- 登陆认证 -->
        <activity
                android:name=".activities.CheckAuthorizationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- TabHost 导航activity -->
        <activity
                android:name=".activities.NavigationTabActivity">
        </activity>
        <!-- 故障分析 -->
        <activity
                android:name=".activities.TroubleAnalyzeActivity">
        </activity>
        <!--电梯调试-->
        <activity
                android:name=".activities.ConfigurationActivity">
        </activity>
        <!--首页-->
        <activity
                android:name=".activities.HomeActivity">
        </activity>
        <!--固件管理-->
        <activity
                android:name=".activities.FirmwareManageActivity">
        </activity>
        <!--帮助-->
        <activity
                android:name=".activities.HelpSystemActivity">
        </activity>
        <activity
                android:name=".activities.ParameterGroupActivity">
        </activity>
    </application>

</manifest>